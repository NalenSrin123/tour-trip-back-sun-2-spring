spring.application.name=tour-trip-api

# closing Data create auto-update in the database, because i will use flyway to manage the database
spring.jpa.hibernate.ddl-auto=validate
# processing flyway to run migrate data that i was made in the db/migration and db/seed folder
spring.flyway.locations=classpath:db/migration,classpath:db/seed